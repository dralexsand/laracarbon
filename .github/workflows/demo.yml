name: Demo Workflow
on: workflow_dispatch
jobs:
  print:
    runs-on: ubuntu-latest
    steps:
      - name: print
        run: echo Hello GH Actions!
      - name: print a few lines
        run: |
          echo First line
          echo Second line
    
    
